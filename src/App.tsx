import React from 'react';
import {QuizPlay} from "./component/QuizPlay";

const App: React.FC = () => {
    return (
        <div>
            <QuizPlay />
        </div>
    );
};

export default App;
